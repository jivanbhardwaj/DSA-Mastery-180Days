#include <bits/stdc++.h>
using namespace std;
void summationOfTwoNumbers() {
int a, b; // Declare two integer variables
cin >> a >> b; // Read two integers from input
    // Print the sum of the two integers
 cout <<  a +  b << endl;
}
/**
 * @brief Reads two integers from input and prints their sum.
 *
 * The program defines a function that reads two integers from standard input,
 * calculates their sum, and prints the result. It demonstrates basic input/output
 * operations in C++.
 *
 * @return int Returns 0 upon successful execution.
 */
int main() {
    // Call the function to print their sum 
    summationOfTwoNumbers();
    summationOfTwoNumbers();
    summationOfTwoNumbers();
    return 0;
}