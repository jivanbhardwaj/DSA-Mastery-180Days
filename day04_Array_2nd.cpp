#include <bits/stdc++.h>
using namespace std;

int main() {
    int bob[5]; // Array of integers
    for(int i = 0; i <= 4; i++) {
        cin >> bob[i]; // Input each element of the array
        cout << bob[i] << endl;
    }
    return 0;
}